export const metadata = {
  title: "Sign Up | Lilijon Hotel",
  description: "Create your Lilijon Hotel account to enjoy exclusive benefits.",
  robots: {
    index: true,
    follow: true,
  },
};

export default function SignUpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
